import 'package:flutter/material.dart';
import 'package:get/get.dart';

class PostImgPage extends StatelessWidget {
  const PostImgPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('this is post image page'),
    );
  }
}
